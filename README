
Install node and npm
    $ brew install node
    $ node -v
    $ npm -v

Install dependencies
    $ npm install

Run server
    $node server.js

Open web app at:
    localhost:8080

You can all see all the raw json data or search endpoint at:
http://localhost:8080/api/artists/search?keywords=taylor


Tech stack
Node
npm
bootstrap
React
express
body-parser